/**
 * \brief Web stub — QtQuick3D InstanceList replacement for JQML.
 *
 * Property-only stub; on the desktop the real QtQuick3D type is used.
 */
import QtQuick 2.15

QtObject {
    property int instanceCount: 0
    default property list<QtObject> instances
}
