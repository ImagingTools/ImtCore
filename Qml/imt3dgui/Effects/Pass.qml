/**
 * \brief Web stub — QtQuick3D Pass replacement for JQML.
 *
 * Property-only stub; on the desktop the real QtQuick3D type is used.
 */
import QtQuick 2.15

QtObject {
    property var shaders: undefined
    property var commands: undefined
    property var output: undefined
}
