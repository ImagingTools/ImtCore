/**
 * \brief Web stub — QtQuick3D Skeleton replacement for JQML.
 *
 * Property-only stub; on the desktop the real QtQuick3D type is used.
 */
import QtQuick 2.15
import imt3dgui 1.0

Node3D {
    default property list<QtObject> joints
}
